# BACKLOG — TYBot

_최종 갱신: 2026-08-27_

이 파일은 **다른 세션·다른 에이전트(Codex 등)·다른 사람에게 넘기는 인계 문서**다.
아이디어는 여기 쌓고, 구현은 준비된 순서대로 한다.

## 이 파일을 읽는 에이전트가 먼저 할 일

1. [`AGENTS.md`](AGENTS.md) — 절대 원칙 8개. **위반하면 리뷰에서 반려된다**
2. `git config core.hooksPath .githooks` — 커밋 가드 활성화 (도구 무관)
3. `pip install -e ".[dev]"` → `pytest` (전부 통과해야 시작) → `ruff check src tests scripts`
4. 아래에서 **`상태: 준비됨`** 인 항목 중 우선순위 높은 것을 고른다
5. 작업 전 이 파일의 해당 항목을 `상태: 진행중 (담당/날짜)` 로 바꿔 커밋 → **중복 작업 방지**
6. 완료하면 `상태: 완료 (커밋해시)` 로 바꾸고, 배운 것을 `참고` 에 남긴다

### 상태 값
| 값 | 뜻 |
|---|---|
`준비됨` | 의존성 해결됨. 지금 착수 가능 |
`대기` | 앞선 항목이나 외부(보안·DBA·플랜) 결정을 기다림 |
`진행중` | 누가 잡고 있음. 담당·날짜 표기 |
`완료` | 커밋 해시 표기 |
`보류` | 하지 않기로 결정. **이유를 남긴다**(다시 제안되는 것을 막기 위해) |

---

## 우선순위 요약

| ID | 제목 | 우선 | 상태 | 의존 |
|---|---|---|---|---|
| [B-01](#b-01) | 정리 잡 — 스키마 검사·수집 밀림 경보 | **높음** | 완료 (08-25) | 없음 |
| [B-02](#b-02) | 운영 관리 채널(`#운영_tybot`) 리포트 | 높음 | 준비됨 | B-01 |
| [B-03](#b-03) | PostgreSQL 인덱서 (원문 → `raw_line`) | 높음 | 준비됨 | DB 설치 |
| [B-04](#b-04) | 조직·인사 스냅샷 반영 잡 | 높음 | 대기 | 보안 승인, B-03 |
| [B-05](#b-05) | Slack 이메일 → 사번 매핑 | 중 | 대기 | B-04 |
| [B-06](#b-06) | 조직 트리 기반 권한 상속 | 중 | 대기 | B-05 |
| [B-07](#b-07) | PII 사후 스캔 + 승인 큐(묘비) | 중 | 대기 | B-02 |
| [B-08](#b-08) | 관리 콘솔 (React + FastAPI) | 중 | 진행중 | 승인됨(2026-08-25) |
| [B-09](#b-09) | 중앙 아카이브 Git 저장소 연결 | 중 | 준비됨 | 없음 |
| [B-10](#b-10) | 일일·주간 요약 정기 발행 | 낮음 | 준비됨 | B-02 |
| [B-11](#b-11) | 스캔 PDF·구형 hwp 변환 | 낮음 | 대기 | 사람 승인 흐름 |
| [B-12](#b-12) | 임베딩 검색(pgvector) | 낮음 | 대기 | B-03 |
| [B-13](#b-13) | 단일 인스턴스 락 | 낮음 | 준비됨 | 없음 |
| [B-14](#b-14) | 분류 프롬프트 비용 절감 | 낮음 | 준비됨 | 없음 |
| [B-15](#b-15) | Slack 표시 이름 일괄 변경 도구 | 낮음 | 보류 | — |
| [B-16](#b-16) | 콘솔 쓰기 API + 배포·규칙 반영 러너 | **높음** | 대기 | PostgreSQL 설치 |
| [B-17](#b-17) | 규칙 편집 QA — 수정 전후 답변 비교 | **높음** | 준비됨 | B-16 |
| [B-18](#b-18) | 워크스페이스 안내 절차화 + 화면 캡처 | 중 | 준비됨 | 캡처 이미지 |
| [B-19](#b-19) | 이상 사용량 감지 API | 중 | 준비됨 | 없음 |
| [B-20](#b-20) | 임시 관리자 계정(admin/1111) 제거 | **높음** | 대기 | B-16 |
| [B-21](#b-21) | 캔버스 수집 실환경 검증 | **높음** | 준비됨 | 없음 |
| [B-22](#b-22) | 캔버스 편집 실시간 반영 | 낮음 | 대기 | B-21 |
| [B-23](#b-23) | 표준 업무 채널 생성 바로가기·명령 | 높음 | 진행중 (코드 완료·Slack 반영 대기, Codex/2026-08-27) | 없음 |
| [B-24](#b-24) | 관리자 환경변수 설정 화면 | 높음 | 완료 (Codex/2026-08-27) | B-08 |
| [B-25](#b-25) | 콘솔 '배포' 버튼 (요청 파일 -> root 러너) | 높음 | 준비됨 (봇측 완료) | B-08 |
| [B-26](#b-26) | 답변 파이프라인 2단계 — 도구 호출 + 모델 승급 | 높음 | 준비됨 | B-27 |
| [B-27](#b-27) | 답변 품질 골든셋 (실제 질문 30건) | **높음** | 준비됨 | 없음 |
| [B-28](#b-28) | Hermes 를 요약 프로바이더로 검토 | **낮음** | 보류 (병합으로 대체 → B-29) | B-29 |
| [B-29](#b-29) | **Hermes 아카이브 병합** (합병) | **높음** | 대기 (형식 확인 필요) | 없음 |
| [B-28](#b-28) | 투표 마감 자동 반영 · 명령 늘리기 | 중 | 준비됨 | 없음 |
| [B-26](#b-26) | 복합 기억 질문의 하위 워크스페이스 조회 누락 | 높음 | 완료 (Codex/2026-08-27) | B-07 |

---

## B-01
### 정리 잡 — 스키마 검사·수집 밀림 경보
**우선 높음 · 상태 완료 (2026-08-25) · 의존 없음 · LLM 불필요**

구현: `src/tybot/tidy.py` · `deploy/tybot-tidy.{service,timer}` · `tests/test_tidy.py`
운영 절차: [rocky8.md 7-A2](docs/deploy/rocky8.md)

**참고(배운 것)** — 검사 항목 중 실제로 잡히는 건 대부분 '수집 밀림'이다.
스키마 위반은 writer 가 게시 전 검증을 하므로 드물고, 나오면 사람이 손으로 고친 경우다.
`파싱 안 되는 원문 줄` 검사는 그 손편집을 잡으려고 넣었다.

지금은 아카이브가 조용히 고장나도 아무도 모른다. `상태` 를 사람이 물어야 드러난다.

**할 일**
- `src/tybot/tidy.py` — `python -m tybot.tidy`
- `ArchiveStore.broken()` 으로 스키마 위반 목록 수집
- 채널별 `last_ingested` 확인 → N일 이상 정지한 채널 목록
- 중복·깨진 원문 라인 탐지 (수정하지 말고 **리포트만**)
- `deploy/tybot-tidy.{service,timer}` — 15분 주기
- 결과를 `/var/lib/tybot/reports/tidy-<날짜>.md` 에 기록

**완료 조건**
- [x] 스키마 위반 파일을 일부러 만들고 리포트에 잡히는 테스트
- [x] 오래된 `last_ingested` 를 만들고 밀림 경보에 잡히는 테스트
- [x] **원문을 수정하지 않음**을 확인하는 테스트(파일 바이트 동일)
- [x] `journalctl -u tybot-tidy` 에 요약 1줄
- [x] 리포트가 아카이브 밖에 쓰이고 검색에 안 잡히는 테스트

**주의** — 원문 편집 금지([agent-architecture.md](docs/design/agent-architecture.md) 2절).
정리 잡은 **읽기만** 한다. 리포트는 `archive/` 밖에 쓴다.

---

## B-02
### 운영 관리 채널 리포트
**우선 높음 · 상태 준비됨 · 의존 B-01**

B-01 결과를 사람이 보는 곳으로 보낸다. 마스터봇의 실질적 역할이 이것이다.

**할 일**
- `.env`: `OPS_CHANNEL=#운영_tybot`, `OPS_WORKSPACE=<키>`
- 발행 내용: 워크스페이스별 연결·수집 건수 / 오늘 LLM 비용·상한 대비 /
  스키마 위반 / 수집 밀림 채널 / 근거 못 찾은 질문 Top 10(`qa-log` JSONL 집계)
- 시각: 09:00 · 16:00 (밀림 경보), 17:30 (일일 요약)

**완료 조건**
- [ ] 채널이 없거나 봇이 초대되지 않았을 때 **기동을 막지 않고** 경고만
- [ ] 리포트 본문에 채널명이 들어가도 되는지 판단 — 운영 채널은 관리자 전용이라는 전제를 문서화
- [ ] 봇이 자기 리포트를 다시 수집하지 않음(운영 채널을 수집 제외 목록에)

**주의** — 마지막 항목이 핵심이다. 리포트가 아카이브에 들어가면 요약 재귀가 시작된다.

---

## B-03
### PostgreSQL 인덱서
**우선 높음 · 상태 준비됨 · 의존 DB 설치**

원문이 수천 파일로 늘면 매 질문마다 전부 읽을 수 없다.

**할 일**
- 스키마: [db-and-acl.md](docs/design/db-and-acl.md) 2절 (`raw_line`, `channel`, `audit_query`)
- `src/tybot/index/` — MD → `raw_line` 적재(`content_sha` 로 멱등), `pg_bigm` 검색
- `ArchiveStore.search()` 와 **같은 결과**를 내는지 대조 테스트
- `python -m tybot.index --rebuild` 로 전량 재빌드

**완료 조건**
- [ ] `DROP DATABASE` 후 재빌드로 동일 결과 (DB는 인덱스일 뿐)
- [ ] 출처가 `doc_path` + `line_no` 로 MD 를 가리킴(DB 텍스트가 근거가 아님)
- [ ] 권한 필터가 DB 쿼리 단계에서도 적용됨
- [ ] 한국어 부분일치 확인: `기성금` 으로 `기성금을` 이 검색됨

**주의** — DB 미설치 환경에서도 봇이 동작해야 한다. 인덱서는 **선택 경로**이고,
없으면 파일 스캔으로 폴백한다. DB 선택 근거는 [postgres-vs-mariadb.md](docs/design/postgres-vs-mariadb.md).

---

## B-04
### 조직·인사 스냅샷 반영 잡
**우선 높음 · 상태 대기(보안 승인) · 의존 B-03**

설계·요청서 완료. 보안팀 승인과 DBA 뷰 생성이 선행 조건.

**할 일** — [oracle-sync.md](docs/design/oracle-sync.md) 3·4·6절
- `/var/lib/tybot/inbox` 감시 → `sha256` 검증 → 스테이징 → 트랜잭션 1개로 교체
- 행 수가 기존의 90% 미만이면 중단
- 퇴직·폐지는 삭제가 아니라 `active=false`
- `sync_run` 이력 기록, 실패 시 관리 채널 경고(B-02)

**완료 조건**
- [ ] 손상된 체크섬으로 반영 시도 → 거부 + 직전 스냅샷 유지
- [ ] 90% 미만 스냅샷 → 중단
- [ ] 순환 참조 `parent_code` → 반영 전 검출
- [ ] 반영 파일이 `inbox/processed/<날짜>/` 로 이동

**선행 (우리 밖)**
- [ ] 보안: 방화벽 규칙 1건 — [요청서](docs/deploy/infra-request-snapshot-push.md)
- [ ] DBA: `V_TYBOT_ORG` / `V_TYBOT_EMP` 뷰 + `TYBOT_RO` 계정
- [ ] 내부망: 스냅샷 추출·전송 cron ([export SQL](deploy/sql/) 제공됨)

---

## B-05
### Slack 이메일 → 사번 매핑
**우선 중 · 상태 대기 · 의존 B-04**

**할 일**
- `users.info` 의 `profile.email` ↔ `employee.email` 조인 → `user_identity`
- 답변·요약·감사 기록의 발화자 표기에 `이름(사번)` 적용
- 매핑 실패자는 Slack 표시 이름 그대로. **추정 금지**

**완료 조건**
- [ ] **원문의 발화자 표기는 바뀌지 않음**(원문 불변) — 표시 계층에서만 적용
- [ ] 이메일 없음·중복·불일치 각각의 처리 테스트
- [ ] 개인 이메일(사내 도메인 아님)은 매핑하지 않음

**주의** — 비밀번호를 받는 흐름은 만들지 않는다. 이유는
[identity-and-legacy-login.md](docs/design/identity-and-legacy-login.md) 2절.

---

## B-06
### 조직 트리 기반 권한 상속
**우선 중 · 상태 대기 · 의존 B-05**

현재 권한은 채널 멤버십 + 워크스페이스 화이트리스트 + root 플래그다.
`slack_user → org_code` 가 생기면 계층으로 판정할 수 있다.

**할 일** — [db-and-acl.md](docs/design/db-and-acl.md) 4절
- `share_level`(`org_public` / `org_internal` / `restricted`) 도입
- 하향(상위→하위) 기본 허용, 상향은 `org_public` 만, 형제는 차단

**완료 조건**
- [ ] "옆 현장 자료가 안 보인다" 회귀 테스트
- [ ] 조직 정보가 없으면 상속 없이 자기 채널만(막는 쪽 폴백)
- [ ] 기존 3층 권한([access/__init__.py](src/tybot/access/__init__.py))과 충돌 없이 결합

**결정 필요** — `kind` 별 기본값(team=`org_public`, hq=`org_internal`)으로 갈지. 오너 판단.

---

## B-07
### PII 사후 스캔 + 승인 큐
**우선 중 · 상태 대기 · 의존 B-02**

수집 시점 필터를 통과한 PII 를 사후에 찾는다. **봇이 스스로 지우지 않는다.**

**할 일**
- 정기 스캔 → 후보를 관리 채널에 승인 요청
- 승인 시 해당 라인을 `[삭제됨: PII, 승인자, 일시]` **묘비**로 대체
- 삭제 이력을 감사 로그에

**완료 조건**
- [ ] 승인 없이는 원문이 바뀌지 않음
- [ ] 묘비가 남아 "왜 이 원문이 없나"에 답할 수 있음
- [ ] 묘비 처리 후에도 스키마 검사 통과

**주의** — 원문에 손대는 **유일한 예외**다. 자동 실행 경로를 만들지 않는다.

---

## B-08
### 관리 콘솔 (React + FastAPI)
**우선 중 · 상태 진행중 (읽기 경로 완료, 2026-08-25) · 설계: [console.md](docs/design/console.md)**

**지금까지 된 것**
- 화면 6개 (`console-web/`) — 데이터 현황 · 수집 문서 열람 · API 사용량 · 봇 규칙 편집 ·
  배포 승인 · 워크스페이스 관리
- 읽기 API (`src/tybot/console/`) — **DB 없이 아카이브 MD·감사기록에서 만든다.**
  파일럿 규모에서는 그것으로 충분하고, DB 는 문서가 늘어난 뒤 같은 함수 뒤에 캐시로 넣으면 된다
- 콘솔 인증(토큰 / 인증 프록시 헤더), 봇 상태 파일(`heartbeat.py`)
- 화면 ↔ API 배선 완료. 아직 API 가 없는 구역은 화면에 `예시 데이터` 배지가 붙는다

**남은 것** → B-16 (쓰기 API), B-19 (이상 감지)

**✅ 오너 승인됨 (2026-08-25).** "인바운드 금지" 규칙의 의도는 *봇의 자가학습·편향 방지*였고,
콘솔은 사람이 관리하는 경로라 그 축에 걸리지 않는다. `AGENTS.md` 갱신 완료.

단 네트워크 노출은 별개 축입니다. 현재 배포 서비스는 `127.0.0.1` 에만 바인딩하며,
노출 경로와 접근 통제가 정해지기 전에는 바인딩을 바꾸지 않습니다. 승인권자 1인 제한과
원문 편집 경로 없음 조건도 그대로 유지합니다.

**완료 조건(최소)**
- [ ] 노출 경로와 접근 통제를 오너가 승인하기 전까지 루프백 바인딩 유지
- [ ] 승인 권한이 오너 1인으로 제한됨
- [ ] 배포는 자동 검사 통과 + 승인 + 실패 시 되돌리기
- [ ] 콘솔이 원문을 편집할 수 있는 경로가 없음

---

## B-09
### 중앙 아카이브 Git 저장소 연결
**우선 중 · 상태 준비됨**

지금 원문은 서버 로컬(`/var/lib/tybot/archive`)에만 있다. **재생성 불가 자산인데 백업이 없다.**

**할 일**
- 사내(또는 별도) **비공개** 저장소 생성, 배포키는 **쓰기 권한** 필요
- 15분 주기 commit·push (`ARCHIVE_REPO`, `ARCHIVE_PULL_INTERVAL_MIN` 이미 예약됨)
- force push 금지, 충돌 시 사람 개입

**완료 조건**
- [ ] push 실패가 조용히 넘어가지 않음(관리 채널 경고)
- [ ] 저장소가 비공개이고 접근자가 제한됨
- [ ] 개인 계정이 아니라 조직 소유 저장소 (현재 코드 저장소는 개인 계정 — 이관 계획 필요)

**주의** — 원문에는 사내 대화가 그대로 들어 있다. 코드 저장소와 **반드시 분리**한다.

---

## B-10
### 일일·주간 요약 정기 발행
**우선 낮음 · 상태 준비됨 · 의존 B-02**

- 일일 17:30, 주간 금 16:00
- 산출물은 `archive/derived/` 에. **원문에 되먹이지 않는다**
- 요약은 채널이 아니라 관리 채널 또는 파생 파일로

---

## B-11
### 스캔 PDF·구형 hwp 변환
**우선 낮음 · 상태 대기(사람 승인 흐름 필요)**

현재 미변환으로 목록만 남긴다([convert.py](src/tybot/archive/convert.py)).

- 스캔 PDF: OCR(tesseract + 한국어). **OCR 오류가 사실처럼 굳는 게 문제**라
  인용 시 "원본 확인 권함" 부기가 필수
- 구형 `.hwp`: LibreOffice CLI 변환 검토(의존성 큼)

**착수 조건** — 변환본을 근거로 쓸 때의 표시 규칙을 먼저 정한다. 기술 문제가 아니라 신뢰 문제다.

---

## B-12
### 임베딩 검색 (pgvector)
**우선 낮음 · 상태 대기 · 의존 B-03**

키워드 검색이 부족하다는 **근거가 생긴 뒤에** 한다.
`qa-log` 의 `reason="no_hits"` 질문 목록이 그 근거다(B-02 리포트에 포함).

**완료 조건** — 키워드 검색 대비 개선을 수치로 제시. 안 되면 도입하지 않는다.

---

## B-13
### 단일 인스턴스 락
**우선 낮음 · 상태 완료 (미커밋, 2026-08-25)**

`src/tybot/lock.py`. 콘솔이 재기동 권한을 쥐기 전에 필요해서 앞당겨 처리했다.

작업하며 알게 된 것: **이중 기동이 아니어도 파일 경쟁이 이미 가능했다.**
정기 백필(`tybot-collect.timer`)은 봇과 별도 프로세스인데 같은 MD 에 append 한다.
그래서 락을 셋으로 나눴다 — `instance-bot`, `instance-collect`, `archive-write`(ingest 안에서).

`DATABASE_URL` 이 있으면 Postgres advisory 락, 없으면 파일 락으로 동작한다.

**완료 조건**
- [x] 두 번째 프로세스가 즉시 종료되고 이유를 로그에 남김(보유자 pid·호스트·시각 포함)
- [x] 비정상 종료 후 재기동이 막히지 않음 — OS 가 회수하므로 stale lock 자체가 없다
- [x] 봇과 백필이 서로를 막지 않음

**참고** — `instance_lock("bot").acquire()` 처럼 참조를 남기지 않으면 락 객체가 GC 되면서
파일 핸들이 닫혀 락이 조용히 풀린다. 확보한 락을 모듈 레벨에 붙잡아 두는 것으로 막았다.
테스트(`tests/test_lock.py`)가 이걸 잡아냈다.

---

## B-14
### 분류 프롬프트 비용 절감
**우선 낮음 · 상태 준비됨**

의도 종류를 늘리면서 분류 프롬프트가 ~900토큰이 됐다. 질문당 약 $0.001.
하루 200질문이면 $0.2 — 아직 무의미한 규모지만 커지면 손본다.

**할 일** — 프롬프트 압축, 또는 규칙 기반으로 먼저 걸러 LLM 호출을 줄이기
(현재 `수집`/`전체수집` 만 LLM 미경유).

**주의** — Haiku 프롬프트 캐싱 최소 길이에 못 미쳐 캐싱은 적용되지 않는다.

---

## B-15
### Slack 표시 이름 일괄 변경 도구
**우선 낮음 · 상태 보류**

**보류 이유**: 봇 토큰으로는 타인 프로필 변경이 불가능하고, 관리자 사용자 토큰(`xoxp-`)은
워크스페이스 전체 관리 권한을 가진다. 그 토큰을 상시 온라인 프로세스에 두지 않는다.

필요하면 **봇과 분리된 일회성 스크립트**로 한다(관리자 PC에서 실행, 토큰 미저장).
상세: [identity-and-legacy-login.md](docs/design/identity-and-legacy-login.md) 5절.

B-05(이름 표기)로 요구의 실질은 충족되므로, 이 항목은 정책적으로 꼭 필요할 때만 꺼낸다.

---

## B-16
### 콘솔 쓰기 API + 배포·규칙 반영 러너
**우선 높음 · 상태 대기(PostgreSQL 설치) · 설계: [console.md](docs/design/console.md)**

읽기 API 는 DB 없이 됐지만(파일이 진실이므로), **쓰기는 둘 곳이 필요하다.**
워크스페이스 등록·승인 요청·이력은 파일이 원본인 자료가 아니라서 파일에 쌓으면
동시 수정·복구·감사 추적이 전부 수작업이 된다. 스키마는 이미 준비돼 있다
(`deploy/sql/console_schema.sql`, **서버에서 문법 검증 아직 안 함**).

**할 일**
- 서버에 PostgreSQL 16 설치 → 스키마 적용 → `DATABASE_URL` 설정
- **콘솔 계정을 `console_user` 테이블로 이관** — 지금은 `CONSOLE_ACCOUNTS` 환경변수이고,
  설정이 비면 임시 계정 `admin`/`1111` 이 열린다. 비밀번호 변경·계정 추가를 화면에서 하려면 DB 가 필요하다
- 쓰기 엔드포인트: 워크스페이스 등록/토큰 교체, 승인·반려, 되돌리기
- 배포 러너: 검사 실행 → fast-forward 검증 → pull → 재기동 → 90초 헬스체크 → 실패 시 되돌리기
- 규칙 반영 러너: 파일 교체 + 이전 버전 보관(`harness_version`)

**쓰기 엔드포인트를 붙일 때 — CSRF 방어가 필요하다**
지금은 읽기뿐이라 쿠키 인증만으로 충분하다(`SameSite=strict` 로 다른 사이트에서 요청을 보낼 수 없다).
상태를 바꾸는 POST 를 열면 그것만으로는 부족하다. CSRF 토큰이나 `Origin` 검사를 함께 넣는다.

**먼저 정해야 할 것 — 시크릿 암호화 키를 어디에 두나**
봇/앱 토큰을 DB 에 암호화해 넣기로 했는데(제한 B), 키 보관처가 미정이다.
후보: systemd `LoadCredential`, 서버 파일(0400) + 백업 정책, KMS.
**키가 DB 옆에 평문으로 있으면 암호화한 의미가 없다.**

**완료 조건**
- [ ] 임시 계정 `admin`/`1111` 이 더 이상 쓰이지 않는다
- [ ] 콘솔에서 워크스페이스를 등록하면 봇이 재기동 없이 그 워크스페이스를 잡는다
- [ ] 등록 오류가 그 워크스페이스만 멈추고 다른 봇은 계속 뜬다
- [ ] 저장된 토큰을 다시 읽는 경로가 서버에도 없다
- [ ] `deploy_event`·`archive_read_audit` 에 UPDATE/DELETE 권한이 없다
- [ ] 배포 실패 시 자동으로 직전 상태로 돌아간다

---

## B-17
### 규칙 편집 QA — 수정 전후 답변 비교
**우선 높음 · 상태 준비됨 · 의존 B-16**

지금은 규칙 MD 를 고쳐도 **봇 답변이 어떻게 달라지는지 반영 전에는 알 수 없다.**
승인하는 사람도, 요청한 사람도 결과를 못 보고 판단한다.

API 문서에서 요청을 보내 응답을 확인하듯, 콘솔에서 규칙을 바꾸고 질문을 한 번 던져
**바뀐 규칙으로 나온 답변**을 바로 볼 수 있게 한다.

**화면 구상**
- 봇 규칙 편집 화면에 `시험 질문` 칸 추가
- 질문을 넣으면 두 답변을 나란히 보여 준다
  - 왼쪽: **현재 규칙**으로 답한 결과
  - 오른쪽: **편집 중인 규칙**으로 답한 결과
- 사용한 근거(출처)와 비용도 함께 — 규칙 변경이 검색 결과까지 바꿨는지 보이게

**설계에서 주의할 것**
- 이 시험 호출은 **실제 LLM 비용을 쓴다.** 사용량에 별도 항목으로 집계하고,
  워크스페이스 상한을 그대로 적용한다. 무제한으로 눌러 볼 수 있으면 안 된다
- 시험 답변을 **아카이브에 저장하지 않는다**(원칙 1 — 요약 재귀 금지).
  감사기록에는 `reason=rule_test` 로 남긴다
- 답변 근거는 **요청자의 권한 범위**로 좁힌다. 시험이라는 이유로 권한이 넓어지면 안 된다
- 편집 중인 규칙은 아직 승인 전이므로 **디스크에 쓰지 않고 메모리로만** 넘긴다

**완료 조건**
- [ ] 규칙을 바꾸고 질문 하나로 전후 답변을 비교할 수 있다
- [ ] 시험 호출 비용이 사용량에 잡히고 상한에 걸린다
- [ ] 시험 답변이 아카이브에 저장되지 않는다
- [ ] 승인 화면에서도 요청자가 돌려 본 시험 결과를 볼 수 있다

---

## B-18
### 워크스페이스 안내 절차화 + 화면 캡처
**우선 중 · 상태 준비됨**

`워크스페이스 관리` 화면의 Slack 앱 만들기 안내가 **문장형이라 따라 하기 어렵다.**
줄글 대신 번호가 매겨진 짧은 절차로 바꾼다.

```
전) api.slack.com/apps 에 접속해 Create New App → From an app manifest 를 고릅니다.
    봇을 붙일 워크스페이스를 선택하고, 형식은 YAML 을 선택한 뒤 아래 내용을 붙여넣습니다.

후) (1) api.slack.com/apps 접속
    (2) Create New App → From an app manifest
    (3) 본인이 생성한 워크스페이스 선택
    (4) YAML 선택 후 아래 매니페스트 복사·붙여넣기
```

**할 일**
- 모든 단계를 번호 붙인 짧은 문장으로 분해
- 매니페스트는 **접지 않고 펼친 상태**로 (접혀 있으면 못 찾는다)
- 수정 가능한 값과 **절대 수정 금지** 항목을 분명히 표시
- 2단계에 "Basic Information 에서 **한참 아래로 스크롤**해야 App-Level Tokens 가 보인다" 명시
  — 실제로 여기서 막히는 사람이 많다
- 각 단계에 Slack 화면 캡처 이미지 첨부

**캡처는 사람이 찍어야 한다.** Slack 앱 관리 화면은 로그인해야 보이므로 에이전트가 캡처할 수
없다. 찍어서 `console-web/public/guide/` 에 넣으면 화면에 붙인다.
필요한 컷: ① Create New App 선택 ② 매니페스트 붙여넣기 ③ App-Level Tokens 위치
④ Install to Workspace / Bot User OAuth Token 위치

**완료 조건**
- [ ] 각 단계가 한 문장 이하로 끊어져 있다
- [ ] 매니페스트가 펼친 상태로 보인다
- [ ] 수정 금지 항목이 눈에 띈다
- [ ] 캡처 이미지가 붙어 있거나, 없으면 위치를 글로 정확히 지시한다

---

## B-19
### 이상 사용량 감지 API
**우선 중 · 상태 준비됨**

화면(`API 사용량`, `데이터 현황`)에 이상 감지 영역이 있지만 **아직 예시 데이터다.**
지금은 배지로 그 사실을 표시하고 있다.

**할 일**
- 감지 규칙: 기준선(최근 14일 같은 시각 중위값) 대비 배수, 상한 근접,
  같은 사용자·같은 질문 반복, 수집 중단
- 상태 저장(`anomaly` 테이블) — 확인함/차단됨이 남아야 같은 건이 다시 뜨지 않는다
- 차단(서킷브레이커): 해당 워크스페이스 호출만 멈춘다. 해제는 콘솔에서만

**완료 조건**
- [ ] 감지만 하지 않고 차단까지 한 화면에서 된다
- [ ] 확인 표시한 건이 다시 올라오지 않는다
- [ ] 한 워크스페이스를 막아도 다른 봇은 계속 답한다

---

## B-20
### 임시 관리자 계정(admin/1111) 제거
**우선 높음 · 상태 대기(B-16) · 코드: `src/tybot/console/auth.py`**

`CONSOLE_ACCOUNTS` 가 비어 있으면 콘솔이 `admin` / `1111` 로 열린다.
**오너가 파일럿 편의를 위해 의도적으로 넣은 값이고, 운영 전에 반드시 제거한다.**

이 상태가 왜 위험한가: 콘솔은 봇 토큰(전 워크스페이스 대화 열람)과 배포 권한(원격 코드 반영)을
쥔 화면이다. 콘솔에 닿을 수 있는 사람이면 누구나 같은 계정으로 관리자가 된다.
사내 원격접속망이 없고 노출 경로도 미정이므로(2026-08-25), 기본 계정을 운영에 남기면 안 된다.

**지금 걸려 있는 안전장치** (이미 구현됨 — 지우지 말 것)
- 기본 계정이 쓰이면 기동 로그에 경고
- `/api/me` 가 `usingDefaultAccount: true` 를 내려주고 화면 상단에 빨간 배너
- `CONSOLE_ACCOUNTS` 를 설정하면 임시 계정은 즉시 막힌다
- `src/tybot/console/auth.py` 의 임시 상수에 `TODO(B-20)` 을 표시해 코드에서도 추적한다

**할 일**
- 계정을 `console_user` 테이블로 이관(B-16). 비밀번호 해시는 지금과 같은 scrypt 형식을 쓴다
- 이관이 끝나면 `DEFAULT_USERNAME` / `DEFAULT_PASSWORD` / `default_accounts()` 를 삭제하고,
  계정이 하나도 없으면 **로그인을 열지 않고 기동을 멈추는** 쪽으로 바꾼다
  (아무도 못 들어가는 것이 아무나 들어오는 것보다 낫다)
- `tests/test_console_api.py` 의 기본 계정 관련 테스트 3개를 그에 맞게 고친다

**완료 조건**
- [ ] 저장소 어디에도 `1111` 이 비밀번호로 남아 있지 않다
- [ ] 계정 설정이 비면 콘솔이 기동을 멈추고 사유를 로그에 남긴다
- [ ] 비밀번호 변경·계정 추가를 화면에서 할 수 있다

---

## B-28
### 투표 마감 자동 반영 · 명령 늘리기
**우선 중 · 상태 준비됨 · 코드: `src/tybot/polls.py`, `src/tybot/poll_view.py`**

`/투표` 를 만들었다(2026-08-27, 미커밋). 남은 것과 다음 명령들.

**투표에서 남은 것**
- **마감 시각이 지나도 메시지가 그대로 남는다.** 누가 버튼을 누르는 순간에만 다시 그려진다.
  지금은 마감 후 누르면 "이미 마감된 투표입니다" 안내와 함께 메시지가 갱신되므로 기능은
  맞지만, 아무도 누르지 않으면 채널에는 열린 것처럼 보인다.
  → 정기 잡(`tybot-collect.timer` 옆)에서 만료된 투표를 훑어 `chat_update` 하는 쪽이 맞다.
  `polls.list_polls()` 가 이미 목록을 준다
- 투표 목록·삭제 명령(`/투표 목록`) — 오래된 투표 파일 정리
- 결과를 스레드에 정리해 남기기(원문 아카이브가 아니라 채널 메시지로)

**다음 명령 후보** (`/채널`, `/투표` 와 같은 패턴으로 만든다)
- `/일정` — 회의 시간 잡기. 투표와 겹치므로 투표를 재사용할지 검토
- `/할일` — 채널 단위 체크리스트
- `/찾기` — 아카이브 검색을 명령으로(지금은 멘션으로만 가능)

**명령을 늘릴 때 지킬 것**
1. 도메인 로직은 `src/tybot/<기능>.py` 에 두고 **Slack SDK 를 import 하지 않는다.**
   그래야 Slack 없이 테스트된다(`polls.py` / `poll_view.py` 가 그 예)
2. Slack 배선(`@app.command`, `@app.view`, `@app.action`)만 `slack/pilot.py` 에 둔다
3. 상태는 `<STATE_DIR>` 아래에 둔다. **아카이브에 쓰지 않는다**(원칙 1)
4. 매니페스트를 **두 곳** 고친다 — `docs/pilot/slack-app-manifest.yaml` 과
   `console-web/src/components/SetupGuide.tsx`. `tests/test_manifest_sync.py` 가 대조한다
5. 한글 명령과 영문 예비 명령(`/ty-*`)을 같이 등록한다 — 한글 입력이 막힌 환경 대비

---

## 완료된 것 (참고용 이력)

| 항목 | 커밋 |
|---|---|
| 파일럿 봇 · 아카이브 파이프라인 · 의도 분류 | `11a8932` |
| LLM 의도 분류 + 규칙 폴백 | `2e745e2` 이전 |
| 판단·권고(advice) 경로 | `ced47c6` |
| 감사 기록 + 자동 배포 타이머 | `898799c` |
| 멀티 워크스페이스 + 크로스 열람 화이트리스트 | `2e745e2` |
| 권한 3층 (채널멤버십 / share_with / root) | `d78d64d` |
| 첨부 문서 변환 + 스레드 답글 백필 | `aefe4cc` |
| Oracle 연동 설계 + 인프라 요청서 | `df0c5d4`, `2ce94c8` |
| 백로그·인계 체계 + 도구무관 커밋 가드 + 린트 규칙 고정 | (이번 커밋) |
| `memory` 의도 — "이전 답변 기억나?" 오분류 수정 | `221fb2d` |
| B-01 정리 잡 (스키마·밀림 점검, 읽기 전용) | `4d98f61` |
| 채널 규칙 기반 자동 수집(초대 없이 참여) + 조직코드 기록 | `5a06501` |
| 캔버스 수집(방어적) — **실환경 검증 필요 → B-21** | (이번 커밋) |

---

## B-21
### 캔버스 수집 실환경 검증
**우선 높음 · 상태 준비됨 · 담당 제안: Codex**

구현은 됐지만 **실제 Slack 캔버스로 돌려보지 못했다.** 공식 문서에서 "캔버스 본문을 돌려주는
전용 메서드"를 찾지 못해 파일 다운로드 경로로 구현했고, 응답 형식(마크다운/HTML)과
`canvases:read` 스코프 필요 여부가 미확인이다.

**코드 QA 보강 (2026-08-27, Codex)** — 비지원 MIME·바이너리·API 실패는 미변환으로 막고,
내용 해시 기반 수집 키로 동일 스냅샷 멱등성을 보장했다. 실시간·수동·정기 수집의 채널 규칙도
같은 경계로 통일했다. 가짜 클라이언트 테스트는 통과했지만 아래 실환경 확인은 여전히 필요하다.

검증 절차와 실패 시 수정 지점: [검증 문서 E절](docs/verification/2026-08-25-collection-rules.md)

**완료 조건**
- [ ] 테스트 채널 캔버스로 `@tybot 수집` → `[캔버스본문:...]` 줄이 원본과 일치
- [ ] `canvases:read` 스코프를 빼고도 되는지 / 반드시 필요한지 확정
- [ ] 응답이 마크다운인지 HTML 인지 기록하고 `canvas.py` 주석의 가정 수정
- [ ] 실패 시 `[캔버스:미변환]` 로만 남고 원문이 오염되지 않음을 확인

**설계 의도** — 확인 못 한 형식은 **추측해서 파싱하지 않는다.** 잘못 뽑은 텍스트가 원문에
들어가면 되돌릴 수 없으므로, 미변환으로 남기는 쪽이 항상 안전하다.

---

## B-22
### 캔버스 편집 실시간 반영
**우선 낮음 · 상태 대기 · 의존 B-21**

지금은 `수집` 명령·정기 백필 시점의 **스냅샷**만 남는다. 캔버스가 수시로 편집되면
최신 내용이 아카이브에 늦게 반영된다.

- `file_change` 이벤트 구독 검토(캔버스도 파일이므로 올 가능성)
- 편집이 잦으면 스냅샷이 계속 쌓인다 → 같은 내용은 멱등 처리로 걸러지지만,
  **변경분만 남기는 방식이 필요한지** 판단 필요

**주의** — 원문은 append only 다. 이전 스냅샷을 지우고 갱신하는 방식은 쓰지 않는다.

---

## B-23
### 표준 업무 채널 생성 바로가기·명령
**우선 높음 · 상태 진행중 (코드 완료·Slack 반영 대기, Codex/2026-08-27) · 의존 없음**

비공개 채널은 봇이 사후 발견하거나 자가 참여할 수 없으므로, 수집 대상 채널은 TYBot이
표준 이름으로 직접 생성한다. 공개 채널과 비공개 채널 모두 같은 두문자 규칙을 적용한다.

**할 일**
- 전역 바로가기 `업무 채널 만들기`와 `/채널 생성`이 같은 한국어 모달을 연다
- `본부·실·팀·현장·프로젝트` 선택과 조직명·조직코드·업무명으로 표준 이름을 조립한다
- 생성 요청자와 선택한 참여자를 초대하고 요청자를 논리적 채널 관리자로 기록한다
- `/채널 이름변경`은 최초 생성 요청자 또는 승인된 관리 사용자만 실행한다
- 앱 매니페스트·Pro 플랜 운영 절차와 재설치 필요 사항을 문서화한다

**완료 조건**
- [ ] 공개·비공개 채널 생성 모두 TYBot이 처음부터 멤버다
- [x] 규칙 밖 이름은 API 호출 전에 거부한다
- [x] 이름 변경 권한이 없는 사용자는 봇 권한을 대신 쓸 수 없다
- [x] Socket Mode를 유지하며 새 인바운드 포트를 열지 않는다
- [x] 모달·명령·소유권 저장 단위 테스트

**코드 QA (2026-08-27)** — 전체 `303 passed`, `ruff check` 통과. 첫 항목은 각 워크스페이스
앱에 갱신 매니페스트를 적용·재설치한 뒤 실제 공개/비공개 채널을 하나씩 만들어 확인한다.

---

## B-24
### 관리자 환경변수 설정 화면
**우선 높음 · 상태 완료 (Codex/2026-08-27) · 의존 B-08**

서버 환경파일 전체를 자유 편집하지 않고, 워크스페이스 관계와 봇 동작에 필요한 허용 항목만
관리 콘솔에서 설정한다. 시크릿 원문은 이 API가 읽거나 반환하지 않는다.

**할 일**
- owner 전용 `환경변수 설정` 메뉴와 API
- `ROOT_WORKSPACES`·`CROSS_WS_READ`·워크스페이스 라벨 편집
- `REALTIME_INGEST`·`AUTOJOIN_CHANNELS`·`REPLY_IN_THREAD` 편집
- 관리 오버레이 파일 원자 저장, 변경 감사 로그, 봇 재시작 요청
- 임시 `admin/1111` 계정에서는 쓰기 차단, Origin/CSRF 검사

**완료 조건**
- [x] member와 미인증 요청은 설정 조회·수정 모두 차단
- [x] API 응답·감사 로그에 토큰/API 키가 포함되지 않음
- [x] 모르는 워크스페이스나 자기 자신 크로스 열람은 저장 전에 거부
- [x] 기존 `/etc/tybot/tybot.env`와 원문 아카이브를 수정하지 않음
- [x] 저장 후 봇이 관리 오버레이를 읽고 재시작

---

## B-26
### 복합 기억 질문의 하위 워크스페이스 조회 누락
**우선 높음 · 상태 완료 (Codex/2026-08-27) · 의존 B-07**

`이전 답변 기억나? 그리고 전산팀은 무슨 일이 있어?`처럼 기억 여부와 업무 조회가 한 메시지에
들어오면 memory 응답만 반환하던 문제를 수정했다. 기억 질문과 별도 질문을 분리하고, 명시된
워크스페이스의 원문만 요약 근거로 전달한다.

**완료 조건**
- [x] 순수 기억 질문은 기존 memory 응답 유지
- [x] 복합 질문의 별도 업무 질문을 버리지 않음
- [x] 명시된 워크스페이스 외의 원문을 요약 근거에 섞지 않음
- [x] root·화이트리스트 권한 필터는 답변 생성 전에 그대로 적용

---

## B-25
### 콘솔 '배포' 버튼 — 서버 터미널 없이 최신 코드 반영
**우선 높음 · 상태 준비됨 (봇측 완료, 콘솔 API·UI 남음) · 의존 B-08**

매번 서버에 SSH 로 들어가 `git pull` + `install.sh` 를 치는 것이 번거롭다는 요청.

**절대 하지 말 것 — 콘솔이 직접 명령을 실행하게 만들기.**
배포는 `git pull` + `/opt` 쓰기 + `systemctl restart` 로 전부 root 다. 콘솔은
`User=tybot` 으로 돌고 웹에 노출된 표면이다. 거기에 root 실행 권한(sudo·polkit·setuid)을
주면 콘솔 취약점 하나가 서버 장악이 된다. **파일 기반 요청으로만 연결한다.**

### 이미 만들어진 것 (봇측, 이 항목의 절반)
| 파일 | 역할 |
|---|---|
| `src/tybot/deploy_request.py` | 요청 생성·소비, 상태 읽기/쓰기, 쿨다운·중복 차단 |
| `deploy/deploy-runner.sh` | root 러너. flock 직렬화 → `update.sh` 호출 → 상태 기록 |
| `deploy/tybot-deploy.path` | `deploy-request.json` 생성 감지 |
| `deploy/tybot-deploy.service` | 러너 실행(root). 브랜치·경로가 **유닛에 고정** |
| `tests/test_deploy_request.py` | 12건 |

`update.sh` 가 테스트 게이트를 이미 갖고 있다 — **테스트 불통 커밋은 배포되지 않고
운영 프로세스는 그대로 유지된다.** 자동 배포의 유일한 안전장치이므로 우회하지 말 것.

### 남은 일 (콘솔 담당)
1. `POST /api/deploy` — `role=admin` 만. 본문은 선택 `note` 뿐.
   ```python
   from ..deploy_request import request_deploy
   result = request_deploy(actor=session.user, note=body.note or "")
   # result["ok"] 가 False 면 result["reason"] 을 그대로 화면에 띄운다
   ```
2. `GET /api/deploy/status` — `read_status()` 결과를 그대로 반환(시크릿 없음).
   `state` 는 `running | ok | failed | skipped | (없음)`.
3. UI: 버튼 + 진행 표시. `running` 이면 폴링(3~5초). `failed` 면 `message` 와
   `journalctl -u tybot-deploy -n 50` 안내를 함께 보여준다.
4. **브랜치·저장소·경로를 파라미터로 받지 않는다.** 받으면 임의 코드 실행이 된다.
5. 감사기록: 누가 눌렀는지 남긴다(요청 파일의 `actor` 가 러너 로그에 찍힌다).

### 서버 활성화 (사람이 1회)
```bash
sudo systemctl enable --now tybot-deploy.path
```

### 검증 체크리스트
- [ ] member 계정은 `POST /api/deploy` 가 403
- [ ] 연속 클릭 시 두 번째는 쿨다운 사유가 화면에 뜬다
- [ ] 테스트 실패 커밋을 push 하고 배포 → `failed` 이고 봇은 이전 버전으로 계속 동작
- [ ] 새 커밋이 없을 때 → `skipped`
- [ ] 배포 상태 JSON 에 토큰·키가 없다
- [ ] 요청 본문에 브랜치를 넣어도 무시된다

### 대안 (콘솔 작업 없이 지금 되는 것)
`sudo systemctl enable --now tybot-update.timer` — 업무시간 10분마다 자동 배포.
버튼이 필요한 이유는 "지금 즉시" 반영뿐이다.

---

## B-26
### 답변 파이프라인 2단계 — 도구 호출 + 난이도 기반 모델 승급
**우선 높음 · 상태 준비됨 · 의존 B-27(골든셋 먼저)**

1단계(완료)에서 라벨 1개 -> 하위질문 N개로 바꿨다. 2단계는 **어떤 능력을 쓸지 LLM 이
정하게** 한다. 지금은 하위질문마다 코드가 경로를 고정한다.

```
질문 -> [저가 모델] 도구 선택 + 난이도 판정
     -> 도구 실행(코드가 실행, 권한도 코드가 적용)
     -> [난이도에 따라 저가/고급 모델] 근거로 답변 1개
```

도구(전부 **읽기 전용**): 아카이브 검색 / 기간 스캔 / 봇 상태 / 정책 설명 / 워크스페이스 목록

### 지켜야 하는 선 — 어기면 '하지 않기로 한 것' 에 걸린다
| 선 | 이유 |
|---|---|
| 도구는 읽기 전용. 수집·채널 생성·설정 변경은 LLM 재량 밖 | 아카이브 원문은 사람이 쓴 텍스트다. 그것을 읽는 모델에 쓰기 도구를 주면 프롬프트 인젝션이 곧 권한이 된다 |
| ACL 은 LLM 이 내용을 보기 **전에** 코드가 적용 | 권한을 모델 판단에 맡기면 원칙 3(막는 쪽이 기본값)이 무너진다. 권한 범위가 도구 인자로 넓혀질 수 없어야 한다 |
| 출처 검증은 생성 **후** 코드가 | 원칙 2. 출처 없는 문장은 내보내지 않는다 |
| 정규식 폴백 유지 | 모델 장애(예: 401) 때 `상태`·`도움말` 은 답해야 한다 |

### 비용
현재 질문당 2회 호출(분해 + 답변). 도구 루프로 1~3회 늘어난다. 저가 모델 계획 +
고급 모델 최종 조합으로 질문당 약 $0.01 추정. 난이도 판정으로 단순 질문은 저가 모델에서 종료.

---

## B-27
### 답변 품질 골든셋 — 실제 질문 30건
**우선 높음 · 상태 준비됨 · 의존 없음**

파이프라인이 LLM 판단에 더 의존할수록 회귀를 눈으로 잡을 수 없다. **문장을 검증하지 않고
행동을 검증한다.**

`qa-log` 에서 실제 질문 30건을 골라 각 건에 대해 고정한다:
- 어떤 하위질문으로 분해됐는지(kind 목록)
- 아카이브 답변에 `출처:` 가 붙었는지
- 권한 밖 문서가 인용되지 않았는지
- 모델 장애를 주입했을 때도 답이 나가는지

문장 내용은 단정하지 않는다 — 모델이 바뀌면 문장은 달라지고, 그걸 고정하면 테스트가
바뀔 때마다 깨지는 소음이 된다.

### 함께 넣을 것 — 규칙 대 LLM 불일치 로깅
규칙 분해(`plan_by_rule`)는 비용이 0이다. 매 요청에서 둘 다 계산하고 **다를 때만** 기록한다.
이것이 라벨된 데이터가 되어 폴백 규칙 개선과 프롬프트 수정의 재료가 된다.

**자동 반영은 하지 않는다.** LLM 출력을 규칙으로 자동 반영하는 것은 봇이 자기 출력을
되먹여 학습하는 것이고, 인바운드 포트 금지 규칙과 같은 축에서 금지된다(CLAUDE.md).
사람이 주 1회 보고 결정한다: 규칙 보강 / 프롬프트 수정 / 골든셋 추가.


---

## B-28
### Hermes 를 요약 프로바이더로 검토 — 마스터 봇 논의 재검토 결과
**우선 중 · 상태 대기(측정 선행) · 의존 B-27**

배경 — 기존 단일 워크스페이스 봇 Hermes(`tec-wmo6240`)가 의도 파악·요약 품질이 더
좋다는 관찰. 봇 개발자가 여럿이라 TYBot 을 상위에 두고 Hermes 를 호출하자는 제안.

> **2026-08-27 갱신 — 우선순위 낮춤.** Hermes 는 TYBot 의 **합병 대상**이고 아카이브를
> 중앙으로 옮긴다(B-29). 데이터가 우리 쪽에 오면 남는 문제는 "왜 Hermes 요약이 더
> 좋은가" 뿐이고 그건 프롬프트·구조를 가져오는 문제다. 상시 크로스 봇 의존을 만들 이유가
> 사라진다. 이 항목은 **병합 전 과도기에만** 의미가 있다.

### 결론: 형태를 구분해야 한다
| | 형태 | 판단 |
|---|---|---|
| A | LLM 이 "어떤 봇을 부를까" 를 자유 판단 | **하지 않는다.** 비결정성·인젝션 → 권한 |
| B | TYBot 이 권한·아카이브를 소유하고 Hermes 를 **정해진 계약의 서비스**로 호출 | 검토 대상 |

B 는 오케스트레이션이 아니라 프로바이더다 — `gateway/providers/` 의 Claude·GPT 자리와
같은 구조다. 개발자가 여럿인 문제의 답은 마스터 봇이 아니라 **인터페이스**다.
봇이 봇을 지휘하면 라우팅 변경마다 팀 간 조율이 필요해져 결합이 더 세진다.

### 모드 1 — Hermes = 요약기 (먼저 검토할 것)
```
TYBot: can_access 로 권한 필터 -> 원문 라인 추출 -> Hermes 에 "이 텍스트를 요약해"
TYBot: 문장만 받고, 출처는 우리가 붙인다
```
**필수 조건: Hermes 가 자기 아카이브를 참조하지 않는다는 보장.**
없으면 Hermes 가 `tec-wmo6240` 원문 지식을 섞고 거기에 **우리 출처가 붙는다.**
권한 유출인데 출처가 위장돼 추적조차 안 되는 가장 위험한 실패 모드다.

### 모드 2 — Hermes = 조회 대상 (나중)
질문을 넘기고 **Hermes 자신의 출처와 함께** 답변을 표시한다. 근거 혼합이 없어 깔끔하지만
선행 조건이 둘이다: 원칙 4(크로스 화이트리스트), **신원 전파**(워크스페이스가 다르면
Slack user ID 가 달라 이메일 매핑이 필요 → B-05).

### 지켜야 할 선
| 선 | 이유 |
|---|---|
| 우리가 먼저 권한 필터한 텍스트만 보낸다 | Hermes 가 스스로 데이터를 가져오면 ACL 이 두 곳으로 갈라진다 |
| 출처는 우리가 붙인다(모드 1) | 원칙 2 |
| Hermes 출력은 아카이브 저장 금지 | 원칙 1(요약 재귀) |
| 호출은 우리 -> Hermes 아웃바운드만 | Hermes 가 우리를 호출하면 인바운드 포트 금지 위반 |
| 장애 시 자체 요약기로 자동 폴백 | 하드 의존이면 남의 배포에 우리 봇이 죽는다 |
| qa-log 에 어느 프로바이더가 만든 문장인지 기록 | 두 팀에 걸친 장애 추적 |

### 착수 전 반드시 할 것 — 측정
골든셋 30건(B-27)을 양쪽에 돌려 비교한다.
- 원인이 **프롬프트·모델 선택** 이면 그것만 가져온다. 크로스 봇 의존 불필요.
  (현재 우리 요약 지시는 "최근 N일 진행 상황을 정리해 주세요" 한 줄이다 — 개선 여지가 크다)
- 원인이 **구조**(채널별 2단 요약, 자체 인덱스 등)면 통합 가치가 있다.

측정 없이 붙이면 "성능이 좋다고 들었다" 로 팀 간 의존을 만들게 된다.

---

## B-29
### Hermes 아카이브 병합 — 합병 대상 데이터 이관
**우선 높음 · 상태 대기(Hermes 쪽 형식·정책 확인 필요) · 의존 없음**

설계: [`docs/design/hermes-merge.md`](docs/design/hermes-merge.md)

되돌리기가 가장 어려운 작업이다. 병합 게이트 5개를 통과하지 못하면 쓰지 않는다.

| 게이트 | 요지 |
|---|---|
| G1 | **봇이 만든 문장은 원문으로 들여오지 않는다.** 들어오면 요약 재귀가 데이터에 굳고 사후 구별이 불가능하다 |
| G2 | 전부 `visibility: private` 로 들어온다. `[공개]`·`share_with` 자동 부여 금지 |
| G3 | PII 스캔은 **병합 전에**. 기존 수집 경로와 **같은 코드**로 |
| G4 | 원문 불변 + `imported_from`·`source_path` 로 출신 기록 |
| G5 | 드라이런 기본, 멱등, 리포트는 `archive/` 밖 |

### 착수 전 필요한 것
Hermes 담당자에게서 **샘플 MD 1건(마스킹)** + 아래 답:
- 아카이브에 봇 답변·요약이 저장돼 있나 (G1 판별 규칙이 여기서 결정된다)
- 프론트매터 필드, 채널·멤버십 정보 유무
- 첨부 원본 보관 여부, 기존 PII 제외 규칙, 전체 규모, 봇 종료 시점

### 전환 시 절대 금지
두 봇이 같은 채널에서 동시 가동 — 중복 답변 · LLM 비용 2배.
Hermes 정지 후 TYBot 설치 순서를 지킨다.

---

## 하지 않기로 한 것 (다시 제안되는 것을 막기 위해)

| 항목 | 이유 |
|---|---|
| 정리 봇이 원문(raw MD) 편집 | 감사 추적 소실·요약 재귀·복구 불가. 다듬기와 왜곡을 코드로 구별할 수 없다 |
| **LLM 이 자유 판단으로** 다른 봇을 지휘하는 오케스트레이션 | 비결정성·디버깅 불가. 외부 텍스트를 읽는 LLM 에 쓰기 권한을 주면 프롬프트 인젝션이 곧 권한이 된다. **다만 정해진 계약의 서비스 호출(프로바이더)은 다르다 → B-28 참조**(2026-08-27 재검토) |
| 봇이 그룹웨어 ID/PW 를 채팅으로 받기 | 비밀번호가 Slack·아카이브에 평문으로 남는다. 이메일 매핑·OTP·SSO 로 대체 |
| 봇 프로세스가 Slack 관리자 토큰 보관 | 워크스페이스 전체 관리 권한이 상시 온라인 프로세스에 놓인다 |
| 워크스페이스별 봇 프로세스 분리 | 원문 파일 경쟁·비용 상한 무력화·중복 답변 |
| 봇이 스스로 `visibility`/`share_with` 변경 | 자료를 워크스페이스 밖으로 내보내는 결정은 사람이 한다 |
| OCR 결과를 표시 없이 원문에 넣기 | OCR 오류가 사실처럼 굳는다 |

각 항목의 상세 근거는 [agent-architecture.md](docs/design/agent-architecture.md),
[identity-and-legacy-login.md](docs/design/identity-and-legacy-login.md) 에 있다.
