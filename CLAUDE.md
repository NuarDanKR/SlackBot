# TYBot — 멀티 워크스페이스 Slack AI 아카이브 봇

> 태영건설 Slack AI TFT. 기존 단일 워크스페이스 Hermes(`tec-wmo6240`)를
> **본부/팀별 멀티 워크스페이스 + 중앙 MD 아카이브 + 멀티 LLM** 구조로 확장한다.

## 한 줄 정의
각 워크스페이스 봇이 대화·첨부를 수집→요약해 **중앙 아카이브(MD)**에 쌓고,
모든 봇은 **권한이 허용된 중앙 아카이브의 원문만 근거로** 답한다. 근거 없으면 답하지 않는다.

## 아키텍처 (기획안 기준)
```
[워크스페이스 A 봇] ─┐
[워크스페이스 B 봇] ─┼─▶ 중앙 서버(DMZ) ─▶ 중앙 MD 아카이브(GitHub 비공개)
[워크스페이스 C 봇] ─┘        │                       ▲
                              └─ LLM 게이트웨이 ─▶ Claude/GPT/... (모델 선택)
                              └─ Legacy 연동(내부망, 읽기전용): 일정·회의실·결재알림
```
- 봇→Slack 은 **Socket Mode(아웃바운드)** — 봇 자체는 인바운드 포트를 열지 않는다.
  이 규칙의 의도는 **봇이 자기 출력을 되먹여 학습·편향되는 것을 막는 것**이다(원칙 1과 같은 축).
  사람이 관리하는 VPN 내부 콘솔은 예외로 승인됨(2026-08-25).
- 중앙 아카이브는 워크스페이스/채널 단위로 **디렉터리·ACL 분리**.
- 봇은 15분마다 아카이브를 pull. "올리지 않으면 옛 자료로 답한다".

## 절대 원칙 (위반 시 데이터 자산이 오염됨)
1. **원문 보존 · 요약 재귀 금지** — 봇 답변/요약을 다시 근거로 저장하지 않는다. 원문만 아카이브.
2. **출처 강제** — 답변 끝에 `출처: #채널, 📄문서(날짜)`. 출처 없으면 = 근거 못 찾은 답.
3. **권한은 막는 쪽이 기본값** — 문서에 `[공개]` 없으면 비공개. 워크스페이스/채널 멤버십으로 답변 필터.
4. **크로스 워크스페이스 조회는 화이트리스트만** — 팀 봇이 타 워크스페이스 자료를 기본 노출 금지.
5. **제외 대상** — 등기부등본·계약자 명단·개인 인적사항·주민번호 등 PII는 아카이브 금지.
6. **시크릿은 저장소 밖** — 토큰/API키는 서버 설정·시크릿 매니저에만. 저장소엔 `.env.example`만.
7. **사람 발언 ↔ 문서 분리** — 숫자가 엇갈리면 문서 우선하되 두 시점 함께 표기.

## 채널 명명 규칙
`#팀명(코드)_업무명` (예: `#팀_자금(ABB540)_주간보고`, `#현장_김해외동(180182)_채팅방`).
봇이 채널명만으로 조직·업무·현장을 분류할 수 있어야 함. → `channel-naming` 스킬로 검증.

## MD 아카이브 스키마
파일별 프론트매터 필수 — `md-archive-schema` 스킬 참조. 원문 블록은 절대 편집 금지.

## 백로그 · 인계
다음 작업은 [`BACKLOG.md`](BACKLOG.md) 에서 고른다. 착수 시 상태를 `진행중` 으로 바꿔 커밋해
다른 세션·에이전트와 중복되지 않게 한다. "하지 않기로 한 것" 목록도 그 파일에 있다.

## 개발 워크플로
- **Claude Code + Codex** 병행. 커밋 메시지/PR은 사람이 검토 후.
- 언어: Python (봇/파이프라인), LLM 게이트웨이는 프로바이더 추상화(LiteLLM 등 검토).
- 테스트: `pytest`(154). 린트: `ruff check src tests scripts`(0건 유지, 규칙은 pyproject 에 고정).
- 커밋 가드: `git config core.hooksPath .githooks` — 도구와 무관하게 시크릿·PII·원문 커밋을 차단.
- 절대 하지 말 것: 봇 프로세스 2곳에서 동시 기동(중복 답변·비용 2배), 중앙 저장소 force push.

## 하위 에이전트 / 스킬 (이 저장소 `.claude/`)
- agents: `slack-integration`, `llm-router`, `md-archiver`, `legacy-connector`
- skills: `md-archive-schema`, `hallucination-guard`, `access-control`, `channel-naming`
- commands: `/new-workspace`, `/archive-review`
