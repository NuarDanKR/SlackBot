# TYBot — 멀티 워크스페이스 Slack AI 아카이브 봇

> 태영건설 Slack AI TFT. 기존 단일 워크스페이스 Hermes(`tec-wmo6240`)를
> **본부/팀별 멀티 워크스페이스 + 중앙 MD 아카이브 + 멀티 LLM** 구조로 확장한다.

## 한 줄 정의
각 워크스페이스 봇이 대화·첨부를 수집→요약해 **중앙 아카이브(MD)**에 쌓고,
모든 봇은 **권한이 허용된 중앙 아카이브의 원문만 근거로** 답한다. 근거 없으면 답하지 않는다.

## 아키텍처 (기획안 기준)
```
[워크스페이스 A 봇] ─┐
[워크스페이스 B 봇] ─┼─▶ 중앙 서버(DMZ) ─▶ 중앙 MD 아카이브(GitHub 비공개)
[워크스페이스 C 봇] ─┘        │                       ▲
                              └─ LLM 게이트웨이 ─▶ Claude/GPT/... (모델 선택)
                              └─ Legacy 연동(내부망, 읽기전용): 일정·회의실·결재알림
```
- 봇→Slack 은 **Socket Mode(아웃바운드)** — 봇 자체는 인바운드 포트를 열지 않는다.
  이 규칙의 의도는 **봇이 자기 출력을 되먹여 학습·편향되는 것을 막는 것**이다(원칙 1과 같은 축).
  사람이 관리하는 VPN 내부 콘솔은 예외로 승인됨(2026-08-25).
- 중앙 아카이브는 워크스페이스/채널 단위로 **디렉터리·ACL 분리**.
- 봇은 15분마다 아카이브를 pull. "올리지 않으면 옛 자료로 답한다".

## 봇 체계 (2026-09-02 확정)
TYBot 이 **마스터**다. 저가 모델이 어느 전문 봇에게 물을지만 정하고,
**권한 판정과 근거 추출은 코드가 소유한다.** 전문 봇은 우리가 권한 필터한 텍스트만 받고
문장만 돌려준다 — 출처는 우리가 붙인다. 첫 전문가는 **Hermes(내부 기록: 회의록·업무 파악·
요약 아카이브)**. 이후 법률·건설·회계. 설계: [`docs/design/bot-hierarchy.md`](docs/design/bot-hierarchy.md)

요약은 **봇이 후보만 만들고 채널 검토자가 확정한다**(자동 반영 금지).
설계: [`docs/design/summary-review.md`](docs/design/summary-review.md)

## 절대 원칙 (위반 시 데이터 자산이 오염됨)
1. **원문 보존 · 검증되지 않은 봇 출력은 근거가 되지 않는다** — 봇이 만든 문장을
   사람이 확인하기 전에 근거로 저장하지 않는다. 아카이브에 쌓는 것은 원문뿐이다.
   실시간 조회로 읽은 대화에도 그대로 적용된다 — **봇 발언은 싣지 않는다.**
   후속 질문("방금 그 문서 다시")도 같다 — 이어 가는 것은 이전 답변 문장이 아니라
   그 답변이 읽은 **원문 좌표**(`EvidenceRef`)이고, 그 좌표는 매번 현재 권한으로
   다시 연다. 설계: [`docs/design/thread-follow-up-evidence.md`](docs/design/thread-follow-up-evidence.md)

   > **2026-09-17 (오너 결정) — 「요약 재귀 금지」 에서 「검증된 것만 다음 단계로」 로.**
   >
   > 이 원칙이 막으려던 것은 봇이 **자기 출력을 되먹여** 편향되는 것이다. 검토자가
   > 승인한 문장은 봇 출력이 아니라 **사람이 확인한 사실**이므로 다음 단계의 입력이
   > 될 수 있다. 다만 「승인받았으니 된다」 만 남으면 같은 재앙이 다른 문으로 들어온다.
   > 파생 요약은 **다섯 조건을 전부** 지킬 때만 만든다.
   >
   > 1. **사람 승인이 전제** — 승인되지 않은 후보는 어떤 파생의 입력도 될 수 없다
   > 2. **원문 좌표 상속** — 파생 문장은 자기가 묶은 항목들의 `evidence_locator` ·
   >    `evidence_message_ts` · `evidence_hash` 집합을 그대로 물려받는다. 출처는
   >    끝까지 원문 링크다(원칙 2). 좌표를 잃은 파생은 만들지 않는다
   > 3. **묶음 상한** — 한 파생 문장이 묶는 항목 수에 상한을 둔다. 사람이 확인할 수
   >    없는 분량이면 승인은 형식이 되고, 그 순간 1번이 무의미해진다
   > 4. **세대 상한 2** — 원문 → 1차 → 2차에서 멈춘다. 2차의 입력은 **항상 1차**이며
   >    다른 2차가 아니다. 코드 규칙이 아니라 스키마 제약으로 박는다
   > 5. **연쇄 stale · 단일 채널** — 물고 있는 1차가 `stale_at` 이 되면 파생도 재검토
   >    대상이 된다. 파생은 한 채널 안에서만 묶는다(여러 채널을 묶으면 일부 원문에
   >    권한 없는 사람이 파생 문장으로 내용을 알게 된다 — 원칙 3)
   >
   > 생성 문장(abstract) 후보도 같은 축이다. 문장은 모델이 써도 되지만 **숫자·날짜는
   > 원문 인용에 있는 것만** 쓸 수 있고, 검토자가 인용과 나란히 보고 승인한다.
   > 설계: [`docs/design/summary-abstraction.md`](docs/design/summary-abstraction.md)
2. **출처 강제** — 답변 끝에 `출처: [조직명]업무명, 📄문서(날짜)`. 출처 없으면 = 근거 못 찾은 답.
   채널명이 아니라 **사람이 부르는 조직 이름**이다(`channels.source_label`) —
   `[전산팀]공지`. 현장은 코드로 부르므로 `[180182]김해외동-채팅방`.
   규칙 밖 이름은 **그대로 남긴다**. 다른 워크스페이스 자료는 뒤에 `(워크스페이스)`.
   근거가 아직 아카이브에 없는 실시간 대화라면 **Slack 메시지 링크**를 출처로 붙인다.
   아카이브 문서로 붙이면 그 문서에 없는 내용이 되고, 사람이 확인하러 갔을 때 못 찾는다.
3. **권한은 막는 쪽이 기본값** — 문서에 `[공개]` 없으면 비공개. 워크스페이스/채널 멤버십으로 답변 필터.
4. **크로스 워크스페이스 조회는 화이트리스트만** — 팀 봇이 타 워크스페이스 자료를 기본 노출 금지.
5. **제외 대상** — 등기부등본·계약자 명단·개인 인적사항·주민번호 등 PII는 아카이브 금지.
6. **시크릿은 저장소 밖** — 토큰/API키는 서버 설정·시크릿 매니저에만. 저장소엔 `.env.example`만.
7. **사람 발언 ↔ 문서 분리** — 숫자가 엇갈리면 문서 우선하되 두 시점 함께 표기.

> **2026-09-11 (오너 결정) — 「아카이브된 것만」 에서 「아카이브 + 실시간」 으로.**
>
> 전에는 아카이브에 들어온 것만 근거로 삼았다. 사람이 봇과 **이어서 묻기** 때문에
> 바꾼다 — 방금 오간 이야기가 검색에 안 잡히면 "모른다" 만 답하게 되고, 그건
> 대화가 아니다.
>
> 넓힌 것은 **근거의 범위**뿐이고 나머지는 그대로다.
> - 권한은 아카이브와 **같은 기준**으로 본다. 요청자가 볼 수 있는 채널만
>   (`specialist_tools.ToolBox._fetch_recent`)
> - 봇 발언은 안 싣는다(원칙 1)
> - 출처는 Slack 메시지 링크로 붙고, 답변에 `[실시간]` 으로 구분해 보인다(원칙 2)
> - 실시간으로 읽은 것을 **아카이브에 쓰지 않는다.** 수집은 마스터 몫이다

## 봇과의 DM 은 개인 작업공간 (2026-09-18, 오너 결정)

DM 은 이제 질문 창구가 아니라 **그 사람 한 명의 기록**이다. 사람 발언·첨부를
`workspaces/<ws>/dm/<user-id>/` 에 쌓고, **본인이 DM 에서 물을 때만** 근거가 된다 —
exec·root 도 예외가 아니고, 본인이라도 채널에서 물으면 나오지 않는다.
봇 답변은 쌓지 않는다(원칙 1). 맥락은 채널보다 강하게 이어 붙이되 끊는 판단은
분해기(LLM)가 한다. 설계: [`docs/design/dm-workspace.md`](docs/design/dm-workspace.md)

## 채널 명명 규칙
`#팀명(코드)_업무명` (예: `#팀_자금(ABB540)_주간보고`, `#현장_김해외동(180182)_채팅방`).
봇이 채널명만으로 조직·업무·현장을 분류할 수 있어야 함. → `channel-naming` 스킬로 검증.

## MD 아카이브 스키마
파일별 프론트매터 필수 — `md-archive-schema` 스킬 참조. 원문 블록은 절대 편집 금지.

## 백로그 · 인계
다음 작업은 [`BACKLOG.md`](BACKLOG.md) 에서 고른다. 착수 시 상태를 `진행중` 으로 바꿔 커밋해
다른 세션·에이전트와 중복되지 않게 한다. "하지 않기로 한 것" 목록도 그 파일에 있다.

## 운영 기능은 콘솔에 둔다 (2026-09-18, 오너 결정)

**새로 만드는 운영 기능은 관리 콘솔에서 쓸 수 있어야 한다.** 서버에 SSH 로 들어가
명령을 치는 것을 전제로 설계하지 않는다.

이유는 편의가 아니다. 서버 명령은 **쓸 수 있는 사람이 한 명**이고, 그 사람이 없으면
기능이 없는 것과 같다. 실제로 그래서 「대기 31건·처리 0건」 이 났다(요약 검토 설계
§왜 밀어 주는가) — 사람은 모르는 일을 하러 찾아오지 않고, 갈 수 없는 자리에는 더욱
가지 않는다.

- 기능을 만들면 **콘솔 화면과 같이** 만든다. CLI 만 만들고 「나중에 붙인다」 하지 않는다
- CLI 는 남긴다. 자동화·디버깅·복구에 쓰이고 콘솔이 그것을 부른다
- **진단 스크립트는 예외다** — 장애 조사와 배포 확인은 서버에서 한다
- 파괴적이거나 비용이 드는 동작은 콘솔에서 **범위를 명시**하게 만든다
  (예: 소급은 채널을 고르게 하고, 실행 전에 분량을 먼저 센다)

## 개발 워크플로
- **Claude Code + Codex** 병행. 커밋 메시지/PR은 사람이 검토 후.
- 언어: Python (봇/파이프라인), LLM 게이트웨이는 프로바이더 추상화(LiteLLM 등 검토).
- 테스트: `pytest`(154). 린트: `ruff check src tests scripts`(0건 유지, 규칙은 pyproject 에 고정).
- 커밋 가드: `git config core.hooksPath .githooks` — 도구와 무관하게 시크릿·PII·원문 커밋을 차단.
- 절대 하지 말 것: 봇 프로세스 2곳에서 동시 기동(중복 답변·비용 2배), 중앙 저장소 force push.

## 하위 에이전트 / 스킬 (이 저장소 `.claude/`)
- agents: `slack-integration`, `llm-router`, `md-archiver`, `legacy-connector`
- skills: `md-archive-schema`, `hallucination-guard`, `access-control`, `channel-naming`
- commands: `/new-workspace`, `/archive-review`
