# TYBot 운영 고정 버전 (파일럿 검증 조합 — 2026-08-19)
# 서버 설치: pip install -r requirements.txt && pip install -e . --no-deps
# 인터넷 없는 서버: deploy/wheelhouse.sh 로 휠 미리 내려받아 옮긴다.

anthropic==0.122.0
slack_bolt==1.30.0
slack_sdk==3.43.0
python-dotenv==1.2.2

# --- 첨부 문서 변환 (xlsx/docx/pptx/pdf/hwpx) ---
openpyxl==3.1.5
python-docx==1.2.0
python-pptx==1.0.2
pypdf==6.16.1
defusedxml==0.7.1

# --- 위 패키지의 전이 의존성 (재현성 고정) ---
annotated-types==0.7.0
anyio==4.10.0
certifi==2026.2.25
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.10
jiter==0.16.0
pydantic==2.11.9
pydantic_core==2.33.2
sniffio==1.3.1
typing-inspection==0.4.1
typing_extensions==4.15.0
et-xmlfile==2.0.0
lxml==6.0.2
pillow==11.3.0
XlsxWriter==3.2.9
