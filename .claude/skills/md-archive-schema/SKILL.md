---
name: md-archive-schema
description: 중앙 MD 아카이브 파일을 만들거나 수정할 때 사용. 프론트매터 필드, 원문/요약 레이어 분리, 출처 표기, 워크스페이스·채널·ACL 메타데이터 표준을 강제한다. "아카이브", "MD 저장", "채널 요약 파일" 작업 시 트리거.
---

# MD 아카이브 스키마

## 파일 위치
`archive/channels/<workspace>/<channel>.md` — 워크스페이스·채널별 1파일.

## 프론트매터(필수)
```yaml
---
workspace: 팀_자금(ABB540)         # 워크스페이스 식별자
channel: "#팀_자금(ABB540)_주간보고"  # 채널명 (명명규칙 준수)
visibility: private                 # public | private (기본 private)
                                    # public = **자기 워크스페이스 안에서만** 멤버십 면제
                                    #          크로스 워크스페이스와 무관하다
acl: [ABB540]                       # 열람 허용 채널/그룹 코드 (같은 워크스페이스 내)
share_with: []                      # 이 문서를 넘길 **다른 워크스페이스** 키 목록(선택)
                                    # 동등 워크스페이스로 나가는 유일한 경로.
                                    # 상위(root) 워크스페이스는 설정으로 열람하므로 불필요
doc_count: 12
last_ingested: 2026-08-19T17:00+09:00
---
```

## 본문 구조 — 요약과 원문을 반드시 분리
```markdown
## 요약 (사람이 관리, 봇은 수정 금지)
- ...

## 원문 (자동 취합, 편집 금지)
> [2026-08-12 09:15] 홍길동: ...
> [2026-08-12 09:20] @Hermes: ... (출처: 📄계약서_v3, 2026-07-31)
```

## 규칙
- **원문 블록은 절대 편집하지 않는다.** 틀린 내용은 `[정정]` 라인으로 옆에 덧붙인다.
- **봇 답변/요약을 원문 블록에 넣지 않는다.**
- `visibility: public` 이 명시되지 않으면 비공개로 취급.
- **같은 워크스페이스라도 소속되지 않은 채널은 답변 대상이 아니다**(공개 채널 포함).
- 크로스 워크스페이스 공유는 `share_with` 로만. `visibility` 로는 열리지 않는다.
- 금액·날짜·기관명·사람 이름은 원문 그대로. 반올림·추론 금지.
- 제외 대상(등기부등본·계약자 명단·PII)은 아카이브하지 않는다.
- 게시 전 이 프론트매터/구조를 검증하고, 실패 시 그날 취합을 롤백.
