[Unit]
Description=TYBot 일정 스냅샷 주기 반영

[Timer]
# 추출기가 1분마다 live 스냅샷을 만든다. 같은 주기로 받는다.
# 밀려도 순서대로(오래된 것부터) 반영되므로 건너뛴 분이 유실되지 않는다.
OnBootSec=2min
OnUnitActiveSec=1min
AccuracySec=10s

[Install]
WantedBy=timers.target
